# radian-rs — Design Notes

`radian-rs` is a greenfield Rust implementation of a 3GPP 5G/6G core (5GC).

These notes capture the early architecture research that scopes the encoding/codec
surface of the project and assesses build feasibility.

## Index

| Doc | Topic |
|---|---|
| [01-asn1-rust-gap-analysis.md](01-asn1-rust-gap-analysis.md) | Rust ASN.1 ecosystem vs. 3GPP needs — how big is the gap? |
| [02-nf-interface-encoding-map.md](02-nf-interface-encoding-map.md) | Full NF / interface ASN.1-vs-JSON split + phased build plan |
| [03-n2-ngap-slice.md](03-n2-ngap-slice.md) | First protocol slice: AMF N2 (NGAP/SCTP) + NAS decode |
| [04-sbi-spine-nrf.md](04-sbi-spine-nrf.md) | SBI spine: HTTP/2 + JSON in sbi-core, and the NRF (TS 29.510) |
| [05-aka-authentication.md](05-aka-authentication.md) | 5G-AKA: AUSF + UDM + the `aka` crypto crate (Milenage, TS 33.501) |
| [06-amf-auth-join.md](06-amf-auth-join.md) | AMF authentication: joins N2 + SBI — discover AUSF, run 5G-AKA, K_SEAF |
| [07-registration-complete.md](07-registration-complete.md) | Complete registration: K_AMF, NAS security, Security Mode + Registration Accept |
| [08-n4-pfcp.md](08-n4-pfcp.md) | User-plane start: N4 PFCP (SMF↔UPF) association + heartbeat via rs-pfcp |
| [09-pfcp-session.md](09-pfcp-session.md) | PFCP Session Establishment: SMF provisions PDR/FAR, UPF allocates N3 F-TEID |
| [10-subscriber-db.md](10-subscriber-db.md) | Subscription store: SubscriberDb/ArpfKeyStore traits + redb persistence (K isolated) |
| [11-gtpu-datapath.md](11-gtpu-datapath.md) | GTP-U datapath: N3 uplink decap, UPF serves N4 + N3, TEID-to-session routing |
| [12-credential-hardening.md](12-credential-hardening.md) | Credential store hardening: gate demo subscriber, redb file 0600 (PR #11 review) |
| [13-encryption-at-rest.md](13-encryption-at-rest.md) | Encryption-at-rest: AES-256-GCM for K/OPc, KEK-injected (HSM seam) |
| [14-pfcp-session-modification.md](14-pfcp-session-modification.md) | PFCP Session Modification: install downlink Outer Header Creation (gNB F-TEID) |
| [15-smf-pdu-session.md](15-smf-pdu-session.md) | SMF as a real NF: Nsmf_PDUSession (Create/Update SMContext) drives N4 establishment + modification |
| [16-amf-pdu-session-leg.md](16-amf-pdu-session-leg.md) | AMF leg: UE NAS-SM → discover SMF (NRF) → CreateSMContext; SMF NRF registration; PR#16 review fixes |
| [17-n2-pdu-session-setup.md](17-n2-pdu-session-setup.md) | N2 PDU Session Resource Setup: N2 SM-info transfer-IEs, learn gNB F-TEID → UpdateSMContext |
| [18-n6-forwarding.md](18-n6-forwarding.md) | N6 forwarding: SMF UE-IP allocation, UPF routes downlink by UE IP, real TUN datapath (N3↔N6) |
| [19-suci-deconcealment-live-ue.md](19-suci-deconcealment-live-ue.md) | Live-UE registration: SUCI→SUPI null-scheme deconcealment + replay UE security capability (free-ran-ue interop) |
| [20-live-pdu-session-signaling.md](20-live-pdu-session-signaling.md) | Live PDU-session signaling: configurable UPF bind + post-registration Configuration Update Command |
| [21-n1-sm-pdu-session-accept.md](21-n1-sm-pdu-session-accept.md) | N1 SM PDU Session Establishment Accept: real 5GSM accept (IP/QoS/AMBR/S-NSSAI/DNN) → live UE completes |
| [22-bdd-datapath.md](22-bdd-datapath.md) | Netns datapath BDD test: cucumber `bdd/` crate drives PFCP+GTP-U through a real UPF+N6 TUN, verifies an ICMP round trip |
| [23-bdd-sim-e2e.md](23-bdd-sim-e2e.md) | Simulator-driven e2e BDD (`@sim`): free-ran-ue gNB/UE + full core → registration → PDU session → ping; AUSF self-registers |
| [24-db-subscriber-nf.md](24-db-subscriber-nf.md) | DB design study: subscriber (UDR) vs NF-profile (NRF) storage — RDB vs NoSQL vs embedded, staged path (redb → Postgres+JSONB; NRF stays DB-less) |
| [25-nrf-heartbeat-expiry.md](25-nrf-heartbeat-expiry.md) | NRF soft state: heartbeat-TTL eviction, assigned heartBeatTimer, register_and_maintain loops in AUSF/SMF |
| [26-udr-nudr-relocation.md](26-udr-nudr-relocation.md) | Subscriber store behind nf-udr over Nudr: SQN split from encrypted creds, AM/SM/SMF-selection JSON documents, stateless UDM (K never on the wire) |
| [27-smf-subscription-data.md](27-smf-subscription-data.md) | SMF reads sm-data/smf-select-data via Nudm_SDM: DNN authorization (403, no N4), subscribed S-NSSAI + session AMBR drive the N1 accept |
| [28-requested-dnn.md](28-requested-dnn.md) | UE-requested DNN: parse the UL NAS Transport 0x25 IE, drive CreateSMContext + N1 accept with it (default `internet` when omitted) |
| [29-pdu-session-reject.md](29-pdu-session-reject.md) | 5GSM PDU Session Establishment Reject: SMF 403 → cause #27 (else #31), NAS-protected DL NAS Transport; negative BDD scenario |
| [30-reject-backoff-timer.md](30-reject-backoff-timer.md) | T3396 back-off IE on the reject: GprsTimer3 encoding (finest-fitting unit, round up), 600s on cause #27, none on #31 |
| [31-requested-snssai.md](31-requested-snssai.md) | UE-requested S-NSSAI: parse the 0x22 IE, slice-keyed subscription validation (403 SNSSAI_DENIED/DNN_DENIED ProblemDetails), 5GSM cause #70 |
| [32-allowed-nssai.md](32-allowed-nssai.md) | Allowed NSSAI at registration: Nudm_SDM am-data, 0x15 IE in the Registration Accept (fail-open), local slice admission at PDU establishment |
| [33-nssai-intersection.md](33-nssai-intersection.md) | Requested-NSSAI intersection: 0x2F IE parsed, allowed = requested ∩ subscribed, rejected NSSAI IE 0x11 (cause: not available in PLMN) |
| [34-registration-reject-62.md](34-registration-reject-62.md) | Registration Reject 5GMM cause #62 when no requested slice is subscribed: rejected NSSAI (0x69) attached, UE context released |
| [35-ue-context-release.md](35-ue-context-release.md) | NGAP UE Context Release Command after the #62 reject (UE-NGAP-IDs pair + NAS cause); multi-PDU downlink answers |
| [36-reg-reject-t3346.md](36-reg-reject-t3346.md) | T3346 back-off IE on the Registration Reject: GprsTimer2 encoding (2s/1min/decihour units), 600s on cause #62 |
| [37-deregistration.md](37-deregistration.md) | UE-initiated deregistration: PFCP Session Deletion, Nsmf ReleaseSMContext, Deregistration Accept (unless switch-off), UEContextReleaseCommand |
| [38-network-dereg.md](38-network-dereg.md) | Network-initiated dereg: UDR DELETE → AMF callback (namf-callback :8001, UE_DIRECTORY) → Deregistration Request (UE terminated) + full teardown |
| [39-t3522.md](39-t3522.md) | T3522: dereg accept-wait, 6s retransmissions (5 sends) then abort; DeregCmd channel, stale-expiry no-ops; live-traced vs free-ran-ue |
| [40-uecm.md](40-uecm.md) | Nudm_UECM serving-AMF tracking: amf-3gpp-access context data in the UDR, withdrawal notifies the stored deregCallbackUri, purge on dereg |
| [41-smf-uecm.md](41-smf-uecm.md) | SMF-side Nudm_UECM (smf-registrations per PDU session, register on create / purge on release) + config-derived AMF callback advertise address |
| [42-uecm-expiry.md](42-uecm-expiry.md) | UECM stale-registration expiry: UDR sweep evicts context data whose serving NF is gone from the NRF (reuses NRF heartbeat), fail-safe on unreachable NRF |
| [43-multi-pdu-ue-ambr.md](43-multi-pdu-ue-ambr.md) | Multiple PDU sessions per UE (sm_refs keyed by psi, release all on dereg) + UE-AMBR from am-data → NGAP UEAggregateMaximumBitRate IE |
| [44-smf-selection.md](44-smf-selection.md) | AMF-side SMF selection by (S-NSSAI, DNN): NRF profile smfInfo + filtered discovery, AMF stores the selected SMF base per session (modify/release hit the same one) |
| [45-per-flow-qos.md](45-per-flow-qos.md) | Per-flow QoS (5QI/ARP/GBR): NGAP QosFlow list in the N2 transfer + NAS Authorized QoS flow descriptions IE (0x79) in the N1 accept, subscription-driven; GBR flow live-verified |
| [46-sbi-oauth.md](46-sbi-oauth.md) | SBI OAuth2 access tokens (TS 33.501): NRF token endpoint, HS256 JWT, UDR enforced via oauth::protect, secretless TokenSource; opt-in via RADIAN_SBI_SECRET |
| [47-pcf-smpolicy.md](47-pcf-smpolicy.md) | Real PCF — Npcf_SMPolicyControl (TS 29.512): sbi_core::npcf policy engine + PcfClient, SMF sources SM policy (AMBR + QoS flows) from the NRF-discovered PCF, deletes on release, falls back to sm-data when no PCF; shared QosFlowPolicy types |
| [48-pcf-udr-policy.md](48-pcf-udr-policy.md) | PCF policy from the UDR (Nudr policy-data, TS 29.519 — DataSet::Policy, per-subscriber, PolicyConfig-shaped doc, local fallback) + Npcf_SMPolicyControl_Update: PCF re-reads UDR on update, SMF refresh-policy trigger re-authorizes a live session; UPF/RAN propagation deferred |
| [49-upf-ambr-qer.md](49-upf-ambr-qer.md) | Session AMBR onto the user plane: SMF installs it as an N4 QER (Create at establishment, Update QER on refresh); UPF polices uplink+downlink with a clock-injected token bucket in the n6 datapath (RateLimited drop); a mid-session change re-rates the policer live. Per-flow GBR + N2/N1 modify still deferred |
| [50-n2n1-pdu-modify.md](50-n2n1-pdu-modify.md) | Mid-session QoS change to the RAN/UE: ngap PDU Session Resource Modify (session AMBR + add-or-modify flows + N1) + nas PDU Session Modification Command (0xCB); AMF namf-comm route → per-association task builds the protected N1 + N2 modify (UeCmd::ModifyPolicy); SMF refresh-policy notifies the serving AMF. Unit/integration-pinned (not @sim-exercised) |
| [51-upf-per-flow-gbr.md](51-upf-per-flow-gbr.md) | Per-flow GBR enforcement at the UPF: SMF installs a per-flow QER (MBR=MFBR) + a classifier PDR (SDF filter = proto+port range) per GBR flow; UPF classifies each packet (transport_key) and polices it against the matched flow's MFBR bucket, else the session AMBR. QosFlowPolicy gains a packet filter; demo = UDP 5000–5010. Establishment-time only; mid-session per-flow re-rate + GFBR/URR deferred |
| [52-upf-per-flow-modify.md](52-upf-per-flow-modify.md) | Mid-session per-flow QoS changes at the UPF: session_flow_modification_request (Create/Update/Remove QER+PDR, stable ids per QFI); handle_n4 applies remove→create→update; SMF refresh-policy diffs old vs new flows (diff_flows) and drives it. RAN/UE add-or-modify already covered by design/50; QoS-flow release toward gNB/UE still deferred |
| [53-qos-flow-release.md](53-qos-flow-release.md) | RAN/UE QoS-flow release: ngap modify gains an N2 QosFlowToReleaseList (cause 5GC-generated), nas modification command appends an N1 delete-flow-description (opcode 2) per released QFI; AMF ModifyPolicy.released_qfis threads them through; SMF refresh-policy computes the fully-gone GBR QFIs and sends releasedQfis. Finishes the release path (design/52) |
| [54-gfbr-urr.md](54-gfbr-urr.md) | GFBR admission control (SMF budget + reserve/release; refuse a session whose GFBR exceeds it → 503 → 5GSM #26; RADIAN_SMF_GFBR_BUDGET_MBPS) + URR usage reporting (UPF session-level volume URR, counts admitted bytes, VolumeMeasurement in the deletion response; SMF logs it). Establishment-time admission + at-deletion reporting; per-flow URRs/thresholds/Nchf deferred |
| [55-sbi-asymmetric-oauth.md](55-sbi-asymmetric-oauth.md) | Asymmetric SBI token signing (ES256/P-256 + JWKS): NRF signs tokens with a private key + serves /oauth2/jwks, resource servers verify via a fetched+cached JWKS (TokenVerifier::Jwks) so a compromised NF can't forge tokens; RADIAN_SBI_OAUTH=asymmetric. HS256 shared-secret mode (design/46) retained. Mutual TLS still deferred |
| [56-sbi-mtls.md](56-sbi-mtls.md) | SBI mutual TLS (TS 33.501 §13.1): sbi_core::tls (TlsIdentity load PEM certs, ServerConfig requires+verifies client cert via WebPkiClientVerifier, run_tls over tokio-rustls+hyper-util; reqwest mTLS client) — rustls **ring** backend (aws-lc-rs unavailable offline). Opt-in on the UDR↔UDM exemplar (RADIAN_UDR/UDM_TLS_DIR). Extending to the NRF/other NFs + rotation/revocation deferred |
| [57-sbi-mtls-mesh.md](57-sbi-mtls-mesh.md) | Full-core mTLS mesh: a process-wide SBI transport (configure_transport/sbi_client/sbi_scheme/sbi_base from one shared RADIAN_SBI_TLS_DIR) so every NF both serves and dials over mTLS; every client constructor uses sbi_client(), registration advertises scheme=sbi_scheme(), NfProfile::service_base() drives discovered client transports (https propagates through NRF discovery). All 7 NFs served via run_tls; replaces design/56 per-NF envs. Live-verified AUSF→UDM→UDR chain over mTLS. Rotation/revocation + PKI bootstrap tool deferred |
| [58-pki-bootstrap-crl.md](58-pki-bootstrap-crl.md) | PKI bootstrap + rotation/revocation: `tools/radian-pki` (init/revoke/rotate over an openssl-ca database — v3 leafs, dual EKU, SAN, chmod-600 keys, real CRL) so a live mTLS core is one `radian-pki init`; sbi_core::tls loads `ca.crl` (fail-closed) and enforces it on BOTH verifiers (revoked client refused at the handshake, revoked server refused by the dialer); `tls::serve` hot-reloads a rotated cert / regenerated CRL on the next accepted connection — live-verified revoke (curl exit 56, no restart) + rotate (serial 1005→1007 live). OCSP/CA-rotation deferred |
| [59-nchf-charging.md](59-nchf-charging.md) | Converged charging: per-flow volume URRs (PER_FLOW_URR_BASE+qfi, partitioned counting — each byte under exactly one URR), session-URR volume threshold → UPF-initiated **Session Report Request** (delta since last report; deletion reports the unreported remainder — no double-billing); SMF N4 rebuilt full-duplex (reader task + pending-map transact) to receive them, acks + relays as Nchf updates; new **nf-chf** (:8007, `sbi_core::nchf`, TS 32.290/32.291 trimmed) keeps CDRs per rating group (0=session, else QFI); SMF as CTF: Nchf create/update/release per PDU session; RADIAN_SMF_USAGE_THRESHOLD_BYTES. Live-verified exact billing (800/540). Quota management, per-flow thresholds, CDR export deferred |
| [60-guti-reregistration.md](60-guti-reregistration.md) | 5G-GUTI re-registration + Identity Response (registration-lifecycle audit slice 1): GUTI_DIRECTORY (5G-TMSI→SUPI) recorded at Registration Accept (fresh GUTI supersedes; survives UE-initiated dereg, dropped on withdrawal); registration_identity classifies Supi/GutiTmsi/Unknown and keeps sec caps + requested NSSAI for the resume; a GUTI hit re-authenticates (fresh 5G-AKA), a miss falls back to Identity Request — whose **Identity Response now has a handler** (was a silent dead end) resuming at authentication. nas: identity_response_suci/supi_from_identity_response, registration_request_with_guti/guti_tmsi_from_registration_request. Security-context reuse (ngKSI), AUTS resync, T3512 deferred |
| [61-sqn-resync.md](61-sqn-resync.md) | SQN resynchronisation / AUTS (audit slice 2): Authentication Failure was unhandled → a drifted-SQN UE could never register. aka compute_auts (USIM: (SQNms⊕AK*)‖MAC-S, f1*/f5*, AMF*=0) + sqn_ms_from_auts (ARPF: verify MAC-S → SQNms, None on mismatch); subscriber-db ArpfKeyStore::resync_sqn adopts the UE SQN (redb-persisted); SBI chain nudr resync endpoint → nudm relay → **nausf resynchronizationInfo in the authenticate request** (relays to UDM before fetching fresh AV, TS 29.509); AMF on_authentication_failure arm resyncs ONCE (resync_attempted guard) via AmfAuth::resync then re-challenges, else aborts+releases; nas authentication_failure_synch/authentication_failure_info (cause #21). Live-verified 403 (bogus AUTS refused at ARPF through AUSF→UDM→UDR) + happy path through real SBI servers. Algorithm negotiation/ngKSI, T3512 deferred |
| [62-nas-algorithm-negotiation.md](62-nas-algorithm-negotiation.md) | NAS algorithm negotiation (audit slice 3): the AMF hardcoded 128-NEA2/128-NIA2 → a UE without them could never complete Security Mode. Now select_algo(cap, priority) picks the strongest algorithm the UE (MSB-first cap byte) + AMF (NEA_PRIORITY=[2,1,3,0], NIA_PRIORITY=[2,1,3] — NIA0 null never offered) both support; establish_security derives algorithm-bound NAS keys (Annex A.8) for the selection + announces it in the SMC. Live-verified: the @sim UE reconfigured to NEA1/NIA1-only registers+pings end-to-end (impossible before). ngKSI/security-context reuse deferred |
| [63-an-release-cm-idle.md](63-an-release-cm-idle.md) | AN release / CM-IDLE (audit slice 4, idle-mode arc slice 1): gNB-initiated UEContextReleaseRequest was unhandled (UE was permanently CM-CONNECTED). Now pfcp session_deactivate_request (Update FAR DROP + clear OHC → UpfState::clear_downlink, route_downlink→None), SMF UpdateSMContext upCnxState=DEACTIVATED → deactivate_up (N4 mod, clear gNB target, session persists), ngap ue_context_release_request build/parse, AMF on_ue_context_release_request deactivates each PDU session's UP + marks CmState::Idle (context retained) + UEContextReleaseCommand. Live-verified SMF↔UPF create→activate→deactivate→re-activate. Service Request resume (slice 2) + paging/DL-buffering (slice 3) deferred |
| [64-service-request-resume.md](64-service-request-resume.md) | Service Request resume (idle-mode arc slice 2): AMF-wide RETAINED store (Mutex<HashMap<5G-TMSI→UeContext>>, keyed by the stable guti_tmsi not the AMF-UE-NGAP-ID) — AN release now MOVES the context there; handle_ngap InitialUEMessage checks the NGAP 5G-S-TMSI IE against RETAINED → on_service_request: verify protected Service Request NAS, restore under a fresh AMF-UE-NGAP-ID (CM-CONNECTED), Service Accept, re-activate each session (AmfSmf::activate_up_connection = Nsmf ACTIVATING → SMF returns retained UPF N3 F-TEID+QoS → N2 PDU Session Resource Setup re-sent). SMF stores n3_addr/snssai + returns N2 info on ACTIVATING (parse_sm_context_created shared with create). nas service_request/service_accept, ngap initial_ue_message_with_stmsi/fiveg_s_tmsi_from_initial_ue. Live-verified SMF ACTIVATING returns the N2 info end-to-end. Paging/DL-buffering (slice 3) + retained-context eviction deferred |
| [65-paging.md](65-paging.md) | Paging + DL buffering (idle-mode arc slice 3 — **arc complete**): network-initiated resume. AN release FAR now BUFF(+NOCP) not DROP; pfcp UpfState buffers downlink for a CM-IDLE session (bounded 64, n6::Downlink::Buffered), first packet → Downlink Data Report (take_dl_data_report); re-activation flushes the buffer to the new tunnel (set_downlink→pending_flush→take_flush, N4 loop encaps on N3). pfcp session_report_request_dldr/parse_dl_data_report; SMF N4 reader routes DLDR → handle_dl_data_report acks + discovers AMF + POST Namf N1N2 (…/n1-n2-messages); AMF page_ue resolves SUPI→retained 5G-TMSI + broadcasts UeCmd::Page to GNB_LINKS registry → each serve_gnb builds ngap::paging (UEPagingIdentity 5G-S-TMSI + TAIListForPaging). Live-verified real UPF accepts BUFF. T3513 retx / T3512 eviction / registration-area paging deferred |
| [66-t3512-implicit-dereg.md](66-t3512-implicit-dereg.md) | T3512 + implicit deregistration (idle-mode robustness): RETAINED no longer leaks. registration_accept now sends T3512 (IEI 0x5E, GprsTimer3, default 54min — the UE periodically re-registers); UeContext.retained_at set at AN release / cleared on resume; a background sweep (RETAINED_SWEEP_SECS=60) calls evict_stale_retained(max_idle) — contexts idle past IMPLICIT_DEREG_SECS (T3512+4min, env RADIAN_AMF_IMPLICIT_DEREG_SECS) are implicitly deregistered: release each PDU session at the SMF (frees UPF session + buffered DL) + purge UECM/GUTI. Live: @sim UE decodes the T3512-carrying accept (IE wire-valid). Two-stage MRT/impl-dereg split + T3513 + periodic-reg handling deferred |
| [67-npcf-am-policy.md](67-npcf-am-policy.md) | Npcf_AMPolicyControl (PCF was SM-only): new `sbi_core::npcf_am` (PolicyAssociationRequest/PolicyAssociation DTOs, AmPolicyConfig demo = RFSP 3 + UE-AMBR 500Mbps/1Gbps, AmPcfState, router create/delete, AmPolicyClient); nf-pcf serves it via npcf::router.merge(npcf_am::router) + advertises npcf-am-policy-control. AMF create_am_policy at SecurityModeComplete (discover PCF → open association) → PCF UE-AMBR OVERRIDES subscribed am-data ue_ambr sent to the gNB (RFSP logged); UeContext.am_policy=(pcf_base,assoc_id) deleted on all teardown paths (dereg/T3522/eviction). Live-verified real nf-pcf serves the endpoint + @sim registers+pings with the override applied. UDR am-policy-data sourcing + UpdateNotify + service-area/RFSP application deferred |
| [68-udr-am-policy-data.md](68-udr-am-policy-data.md) | UDR am-policy-data sourcing (AM analogue of design/48): PCF now sources AM policy per-subscriber from Nudr, not just local config. subscriber-db DataSet::AmPolicy (redb am_policy_data table, keyed (SUPI,"")); nudr GET|PUT /nudr-dr/v2/policy-data/ues/{ueId}/am-data + UdrClient::{get,put}_am_policy_data; npcf_am AmPcfState::with_udr + decide_for(supi) deserializes UDR am-data → AmPolicyConfig (fallback local on none/malformed/unreachable); nf-pcf passes its UDR client; nf-udr provisions demo am-policy-data (RFSP 5, UE-AMBR 300/600 Mbps). Live-verified: demo subscriber → UDR policy (RFSP 5), unprovisioned → local fallback (RFSP 3). UpdateNotify + richer am-policy-data deferred |
| [69-npcf-am-policy-update-notify.md](69-npcf-am-policy-update-notify.md) | Npcf_AMPolicyControl_UpdateNotify — PCF-initiated AM policy change (AM analogue of the SM Update; SBI→N2 bridge like design/50). npcf_am: association store keeps (request, decision); PolicyAssociationRequest.notificationUri (AMF callback, set at create); POST …/policies/{id}/update re-reads UDR via decide_for → 204 unchanged / 200 + push UpdateNotify(PolicyAssociation) to notificationUri over h2c on change (PolicyAssociation/Ambr derive Eq); AmPolicyClient::update. AMF: create_am_policy advertises /npcf-callback/v1/am-policy-notify/{supi}; that route parses ueAmbr→bps, resolves UE_DIRECTORY, sends UeCmd::UpdateAmPolicy to the association task (204/404/400); on_am_policy_update overwrites ctx.ue_ambr + emits a protected ConfigurationUpdateCommand (Generic UE Config Update, TS 24.501 §5.4.4). Live-verified real NRF+UDR+PCF+AMF: unchanged→204, UDR edit→200 new policy + h2c push to AMF with no transport failure. RFSP/service-area-to-RAN + autonomous UDR-change delivery deferred |
| [70-signal-rfsp-to-ran.md](70-signal-rfsp-to-ran.md) | Signal RFSP to the RAN — the AM-policy RFSP index (was only logged, designs 67-69) now reaches the NG-RAN in a UE Context Modification Request (TS 38.413 §9.2.2.7; chosen over Initial Context Setup, which would need the full security context — UECtxMod has only the two UE-NGAP-IDs mandatory, IndexToRFSP/UE-AMBR optional). ngap: ue_context_modification_request(amf,ran,rfsp:Option<u16>,ue_ambr:Option<(u64,u64)>) (proc code 40, IndexToRFSP IE 31 crit ignore + UEAggregateMaximumBitRate) + ue_context_modification_params parser + response builder/parser. AMF: UeContext.rfsp (set from AM policy in on_security_mode_complete); after Registration Accept sends UEContextModificationRequest(rfsp,ue_ambr) to the gNB; UpdateAmPolicy cmd carries rfsp → on_am_policy_update signals RAN (UECtxMod) + UE (ConfigUpdateCmd); handle_ngap logs UEContextModificationResponse. Live loopback (real core + free-ran-ue): UE REGISTERED, AMF "sent UEContextModificationRequest (RFSP)" RFSP 5/UE-AMBR 600-300Mbps (UDR demo), sim receives proc code 40 (unimplemented→ignored, no ACK — design/50/69 precedent). BDD 5/5. Service-Area-List + full Initial Context Setup deferred |
| [71-signal-service-area-to-ran.md](71-signal-service-area-to-ran.md) | Signal the Service Area List to the RAN — the AM-policy service area restriction (allowed/non-allowed tracking areas, TS 23.501 §5.3.4.1) reaches the NG-RAN as an NGAP Mobility Restriction List. Vehicle = DownlinkNASTransport (TS 38.413 §9.2.5.3, which already carries the Registration Accept; the sim FULLY processes it so its decode is a real oxirush↔free5gc wire-compat check, unlike design/70's ignored UECtxMod). npcf_am: ServiceAreaRestriction{restrictionType,tacs} DTO; PolicyAssociation+AmPolicyConfig gain servAreaRes (TS 29.507); demo()=ALLOWED_AREAS TAC 000001; nf-udr provisions demo servAreaRes (sourced via UDR am-policy-data, design/68). ngap: downlink_nas_transport_with_area_restriction(amf,ran,nas,mcc,mnc,allowed_tacs,not_allowed_tacs) (MobilityRestrictionList.serviceAreaInformation, 3-octet TACs, empty side omits IE) + area_restriction_from_downlink_nas parser. AMF: UeContext.area_restriction; parse_tac(6hex→3B)+area_restriction_tacs (buckets ALLOWED/NOT_ALLOWED, drops malformed); on_security_mode_complete builds Registration Accept via the MRL transport when restriction present. Live loopback: AMF "service area restriction — allowed TACs [[0,0,1]]" (UDR-sourced), UE Registration finished (free5gc decoded the MRL). BDD 5/5. Mid-connection servAreaRes change + richer restrictions + full ICS deferred |
| [72-servarea-update-notify.md](72-servarea-update-notify.md) | Mid-connection service area change via UpdateNotify — a servAreaRes edit now rides the design/69 PCF-push path to the RAN. PCF side needed NO code: PolicyAssociation's Eq already includes serv_area_res (design/71), so the update handler's fresh==prev diff fires on a service-area-only change (test pinned). AMF: UeCmd::UpdateAmPolicy gains area_restriction:Option<(allowed,non_allowed)>; am_policy_notify parses policy.serv_area_res via area_restriction_tacs; on_am_policy_update stores ctx.area_restriction (clears when policy has none) + builds the ConfigurationUpdateCommand's DownlinkNASTransport via downlink_nas_transport_with_area_restriction — the updated Mobility Restriction List reaches the RAN on the same message that tells the UE its config changed. Full change sequence: UECtxMod (RFSP+UE-AMBR) then CUC-DL-NAS (+MRL). Tests: npcf_am service-area-only UDR edit still notifies (TAC 000007 pushed); nf-amf handler stores+signals TAC 000002 MRL, RAN reads it back, UE verifies CUC, no-servAreaRes falls back to plain transport+clears. 152 tests; BDD 5/5. LIVE real NRF+UDR+PCF+AMF: servAreaRes-only edit (000001→00000a, rfsp/ueAmbr kept) → 200 + PCF "AM policy changed — notifying the AMF" + h2c push, no transport failure. CM-IDLE UEs still 404 (page-on-policy-change deferred); richer restrictions + full ICS deferred |
| [73-idle-am-policy-update.md](73-idle-am-policy-update.md) | AM policy change for a CM-IDLE UE — hold/page/apply-on-resume (the design/65 downlink-data shape applied to policy). AMF-only: UeContext.pending_am_policy:Option<PendingAmPolicy{ue_ambr,rfsp,area_restriction}> (latest wins); am_policy_notify on UE_DIRECTORY miss searches RETAINED by SUPI → stores the pending change + broadcasts UeCmd::Page(tmsi) over GNB_LINKS → 202 Accepted (only neither-connected-nor-retained still 404s); on_service_request takes the pending change after restoring the context and runs it through on_am_policy_update — the resume emits ServiceAccept + UEContextModificationRequest (RFSP+UE-AMBR) + CUC DL-NAS (+MRL), identical to the CM-CONNECTED change signalling. PCF unchanged (push is fire-and-forget). No T3513 retx (design/65 backlog): un-answered page → change applies at the next natural SR, or dies with T3512 eviction (which deletes the PCF association anyway). Pending survives failed-NAS resume attempts (context re-retained intact). Test: retained UE + real h2c UpdateNotify POST → 202/Page/pending held; protected SR resume → 3 downlinks, ctx carries new AMBR/RFSP/area, RAN reads UECtxMod+MRL, UE verifies both NAS messages. 153 tests; BDD 5/5; live smoke re-run green (free-ran-ue can't go CM-IDLE — integration-tested, design/64/65 precedent). T3513 + registration-area paging + richer restrictions + full ICS deferred |
| [74-t3513-registration-area-paging.md](74-t3513-registration-area-paging.md) | T3513 paging retransmission + registration-area paging (closes the design/65/73 one-shot-broadcast boundary). ngap: supported_tacs_from_ng_setup (NGSetupRequest Supported TA List) + ng_setup_request builder; tac_from_initial_ue (InitialUEMessage ULI NR TAI) + _at builder variants carrying ULI. AMF: GNB_LINKS entries become GnbLink{tacs,tx} — TA list filled when NG Setup arrives (own entry found via UnboundedSender::same_channel); UeContext.tac captured from ULI at registration (both identity paths) + refreshed on SR resume; page_gnbs(tmsi,ue_tac) selection (serving-TA gNBs only; fail-open: empty gNB TA list included, unknown UE TAC broadcasts in AMF_TAC); page_with_retx = T3513 loop (page → sleep → RETAINED-still-there? retransmit, ≤3 attempts; the SR consuming the retained context IS the paging response; exhaustion leaves the context retained for T3512 eviction); T3513=6s env RADIAN_AMF_T3513_SECS; both producers (SMF downlink-data N1N2 + AM-policy UpdateNotify hold) go through spawn_paging; UeCmd::Page gains the TAC → NGAP Paging built with the UE's TA (not fixed AMF_TAC). Tests: ULI/TA-list APER roundtrips; selection (serving+empty-list paged with UE TAC, outside-area not, unknown→broadcast); retx (exactly max_sends then retained stays; early stop on resume). 156 tests; BDD 5/5. LIVE both parsers wire-proven vs free5gc: AMF "gNB serves TACs [[00,00,01]]" (real NG Setup) + "UE 1: registering from TAC [00,00,01]" (real ULI); paging response not sim-drivable (integration-tested). Multi-TAI Registration Area List + paging escalation/DRX + full ICS deferred |
| [75-registration-area-list.md](75-registration-area-list.md) | Registration Area List management (TS 23.501 §5.3.2.3) — a real multi-TAI registration area, assigned at registration, told to the UE, paged across. nas: registration_accept gains registration_area:&[[u8;3]] → 5GS TAI list IE (IEI 0x54, TS 24.501 §9.11.3.9) hand-encoded as ONE type-00 partial list [0|00|count-1][PLMN TBCD ×3][TAC ×3]… capped 16 (tai_list_value; oxirush NasFGsTrackingAreaIdentityList is a raw nas_ie_tlv wrapper) + registration_area_from_registration_accept parser. ngap: paging(tmsi,mcc,mnc,tacs:&[[u8;3]]) now builds a multi-item TAIListForPaging + tacs_from_paging. AMF: UeContext.registration_area assigned by registration_area_for = serving gNB's Supported TA List (own GnbLink via same_channel) ∪ UE TAI, cap 16; Registration Accept carries it; page_gnbs(tmsi,area) pages gNBs whose TA list INTERSECTS the area, each with the FULL area in its TAI list (fail-open: no-NG-Setup gNB included, empty area → all gNBs in AMF_TAC); page_with_retx reads registration_area (fallback [tac]); SR resume from outside the area EXTENDS it (full re-assignment = mobility registration update, not modelled); UeCmd::Page{tmsi,tacs}. Tests: exact §9.11.3.9 octets ([01][99 f9 07][TAC][TAC] for 999/70) + 16-cap; accept roundtrip w/ area; multi-TAI paging roundtrip; area assembly (∪, no dup, degraded); intersect-selection w/ full-area delivery. 158 tests; BDD 5/5 — the real free5gc UE DECODES the TAI-list-carrying accept (wire-compat) + pings. LIVE: AMF "registering from TAC [00,00,01]; registration area [[00,00,01]]" (real NG Setup TA list ∪ real ULI). Mobility registration update + operator TAI-list policy + multi-partial-list encodings deferred |
| [76-mobility-registration-update.md](76-mobility-registration-update.md) | Mobility registration update (TS 24.501 §5.5.1.3) — the real leave-the-area procedure (replaces design/75's extend-on-resume stand-in). nas: registration_request_mobility(mcc,mnc,tmsi) (type 010 mobility updating, GUTI identity, ngKSI 0) + registration_type_from_request (oxirush RegistrationType enum). AMF: on_service_request generalizes into the CM-IDLE-return handler — unprotects ONCE then classifies: ServiceRequest → existing resume (accept + UP reactivation, area-extension tolerance kept); RegistrationRequest type=mobility → registration area RE-ASSIGNED via registration_area_for (new serving gNB's TA list ∪ new TAI) + protected Registration Accept (same GUTI, retained allowed NSSAI, T3512, NEW 5GS TAI list) + NO UP reactivation (sessions stay, UP deactivated until a Service Request — Uplink Data Status not modelled) + pending-AM-policy applies; bad MAC/other → re-retain. GUTI-no-retained-context still falls to design/60 full re-auth (AMF-restart fallback). Test: retained UE (area [000001], 1 session, NSSAI) returns via a gNB serving 000009/00000b from TAC 000009 → one downlink (mobility Registration Accept), 0 SMF activation calls, area re-assigned [000009,00000b], session intact, UE verifies accept + reads new TAI list. 160 tests; BDD 5/5 (registration path shares the restructured dispatch). Mobility not sim-drivable (single-gNB free-ran-ue; design/64/65 precedent). GUTI reallocation + Uplink Data Status + periodic registration (type 011 lightweight path) deferred |
| [77-initial-context-setup.md](77-initial-context-setup.md) | Full Initial Context Setup procedure (TS 38.413 §8.3.1) — the UE context is now established at the gNB EXPLICITLY: ONE InitialContextSetupRequest after Security Mode Complete carrying GUAMI(1/1/0) + Allowed NSSAI + UE Security Capabilities + **K_gNB** (Security Key, 256-bit) + UE-AMBR + IndexToRFSP + MobilityRestrictionList + the protected Registration Accept as NAS-PDU (replaces the DL-NAS(+MRL) + trailing UECtxMod pair of designs 70/71); gNB answers InitialContextSetupResponse. aka: kgnb(kamf, ul_nas_count) (TS 33.501 A.9 FC=0x6E via oxirush derive_kgnb; COUNT = the SM Complete's UL NAS COUNT = sec.ul_count-1 post-unprotect). ngap: InitialContext struct + initial_context_setup_request (proc code 14, hand-built conditional IEs) + initial_context_setup_params + response builder/parser; MRL factored into shared mobility_restriction_list(). AMF: UeContext.kamf (establish_security returns it); on_security_mode_complete emits the single ICS (missing-K_AMF degrades to plain DL-NAS, unreachable); handle_ngap ICS-response arm; fail-open fix: unreachable subscription/PCF no longer clobbers a known UE-AMBR. Mid-connection paths keep their design/69-73 vehicles; reject path keeps plain transport+release. 163 tests; BDD 5/5. LIVE — first ACKNOWLEDGED UE-context procedure: AMF "sent InitialContextSetupRequest (RegistrationAccept)" (NSSAI/RFSP 5/UE-AMBR from UDR policy) → "recv SuccessfulOutcome InitialContextSetup (code=14)" → "gNB established the UE context" → UE "Registration finished" — free5gc decoded the COMPLETE ICS (full APER wire-compat, vs design/70's ignored UECtxMod). ICS-on-resume + NH/NCC + inline PDU sessions + ICS Failure handling deferred |
| [78-ics-on-resume.md](78-ics-on-resume.md) | ICS on idle-resume (closes design/77's first follow-up) — EVERY CM-IDLE return now re-establishes the AS context with an InitialContextSetupRequest carrying a **fresh K_gNB** from the trigger message's UL NAS COUNT (TS 33.501 §6.9.2.1.1): the AN release tore the gNB's AS context down, so the bare-accept DL-NAS of designs 64/76 was spec-short. AMF-only: the CM-IDLE-return handler builds ngap::InitialContext — kgnb(kamf, sec.ul_count-1) (kamf persists in RETAINED since design/77; the SR / mobility RR unprotect advanced the count), retained allowed NSSAI + UE sec caps + UE-AMBR + RFSP + MRL, NAS-PDU = protected ServiceAccept (resume) or mobility RegistrationAccept (new TAI list); labels "InitialContextSetupRequest (ServiceAccept)" / "…(RegistrationAccept — mobility update)"; SR branch still follows with per-session PDUSessionResourceSetupRequest (inline Cxt-Req deferred), mobility reactivates nothing; kamf-less retained ctx degrades to plain DL-NAS + warn; pending AM policy still applies AFTER the ICS (UECtxMod+CUC one message later). All 3 resume tests upgraded: SR → [ICS(ServiceAccept), PDU setup] w/ key==kgnb(kamf,0) + UE verifies the ICS NAS-PDU; mobility → ICS w/ new TAI list + retained NSSAI at the RAN + 0 activations; policy-on-resume → [ICS, UECtxMod, CUC]. 163 tests; BDD 5/5 (live registration ICS unaffected); resume not live-drivable (design/64/65 precedent). Key freshness only (no NH/NCC chains / horizontal K_AMF); ICS Failure + inline PDU sessions deferred |
| [79-nh-ncc-path-switch.md](79-nh-ncc-path-switch.md) | NH/NCC key chains + Xn handover path switch (TS 33.501 Annex A.10 + TS 38.413 §8.4.4) — the AMF maintains the per-UE {NH, NCC} pair and its natural consumer rotates it. aka: nh(kamf, sync_input) (FC=0x6F via oxirush derive_nh; sync = initial K_gNB for NH₁, previous NH after). ngap: path_switch_request (target-gNB side: source AMF-UE-ID, new RAN-UE-ID, ULI, sec caps, per-session new DL F-TEID in PathSwitchRequestTransfer) + path_switch_params (AMF parser) + path_switch_request_acknowledge (SecurityContext{NCC,NH} + switched-session list) + path_switch_ack_security. AMF: UeContext.nh_chain:Option<([u8;32],u8)> = (sync, NCC) — seeded at BOTH ICS points (registration NCC 0; idle-resume RE-seeds NCC 0, new initial AS key); on_path_switch (new handle_ngap arm): resolve by source AMF-UE-ID → move ctx to target (ran_ue_id + TAC from ULI) → NH=KDF(K_AMF,sync), NCC=(NCC+1)%8 (3-bit) → re-point each switched session's UPF downlink via UpdateSMContext (N4 modify) → Ack w/ fresh pair + switched PSIs; unknown UE / unseeded chain ignored. Tests: aka chain determinism/freshness; ngap roundtrips; nf-amf xn_path_switch_rotates_nh_and_repoints_the_downlink (mock SMF sees F-TEID 00000077/10.0.9.2; Ack {NCC 1, NH₁}; 2nd switch chains {NCC 2, NH₂=KDF(K_AMF,NH₁)}; unknown/unseeded → None); ICS tests assert chain seeding. 166 tests; BDD 5/5. Xn switch needs 2 gNBs → not sim-drivable (integration-tested, design/64/65 precedent). Source-gNB release + PathSwitchRequestFailure/ReleasedList + N2 handover deferred |
| [80-release-source-gnb.md](80-release-source-gnb.md) | Source-gNB release + cross-association takeover on path switch (fixes design/79's noted gap AND a latent one its test hid: per-association ues maps mean the PathSwitchRequest lands on the TARGET association while the context lives with the SOURCE — a real two-gNB deployment would miss). ngap: ue_context_release_command_radio (radio-network cause; CauseRadioNetwork::SUCCESSFUL_HANDOVER=2, free5gc's cause for the same message) + release_command_radio_cause parser. AMF: UeCmd::TakeUe{amf_ue_id, reply: oneshot::Sender<Option<Box<UeContext>>>} (dropped UeCmd's unused Clone derive); on_take_ue (source select-loop arm) removes the ctx, replies, and emits UEContextReleaseCommand (successful handover) on ITS OWN association — the Xn completion (TS 23.502 §4.9.1.2); on_path_switch (now takes dereg_tx): local miss → concurrent JoinSet ask to every other GNB_LINKS association (500ms per-ask bound; live loops answer instantly — owner w/ ctx, others None) → insert + proceed; ALSO re-points UE_DIRECTORY to the target association's channel (SBI callbacks would otherwise silently keep hitting the source task post-handover). Tests: ngap radio-cause roundtrip; nf-amf path_switch_takes_over_the_ue_and_releases_the_source (spawned source-association task services TakeUe: ctx moves + NH rotates + Ack correct; exactly ONE release addressed by the OLD RAN-UE-ID w/ cause successful-handover; UE_DIRECTORY same_channel-verified re-point); design/79 local-ctx test unchanged. 167 tests; BDD 5/5. Two-gNB handover not sim-drivable (precedent). PathSwitchRequestFailure/ReleasedList + N2 handover + release-complete correlation deferred |
| [81-n2-handover.md](81-n2-handover.md) | N2 handover (TS 23.502 §4.9.1.3 + TS 38.413 §8.4.1-8.4.3) — the AMF-orchestrated handover across two gNB associations (Xn covered by designs 79/80). ngap 5-message set: handover_required(+params) (target gNB id in TargetID, PSI list, s2t container); handover_request(+params) (intra5GS, UE-AMBR, sec caps, SecurityContext{NCC,NH} for the target's K_gNB derivation TS 33.501 §6.9.2.3.2, allowed NSSAI, GUAMI, per-session UPF UL N3 F-TEID reusing the setup transfer, container); handover_request_acknowledge(+params) (admitted sessions w/ the TARGET's DL F-TEID via HandoverRequestAcknowledgeTransfer, t2s container); handover_command(+params) (relays t2s to the source); handover_notify(+params) (UE arrived + ULI); gnb_id_from_ng_setup + ng_setup_request gains gnb_id param. AMF: GnbLink.gnb_id (from NG Setup — target resolution key); UeCmd::Forward{pdu,label} (cross-association send); HANDOVERS static (PendingHandover: source tx+ran_ue_id, rotated {NH,NCC}, admitted) created@Required/filled@Ack/consumed@Notify; on_handover_required (source assoc): resolve target by gNB id → rotate NH (burned on failure) → fetch UL F-TEIDs via activate_up_connection (idempotent) → Forward HandoverRequest; on_handover_request_ack: record admitted → Forward HandoverCommand to source; on_handover_notify (target assoc): TakeUe from source (which releases w/ successful-handover, design/80) → apply NH/RAN-UE/TAC → UpdateSMContext per admitted DL F-TEID → UE_DIRECTORY re-point. Tests: all 5 roundtrips; full two-association orchestration (HandoverRequest carries NCC1/NH₁+UL F-TEID; source gets the command w/ old RAN-UE 4; notify moves ctx, re-points DL to 000000aa/10.0.9.5, source released, directory re-pointed, HANDOVERS consumed). 169 tests; BDD 5/5. LIVE: gNB-id parse wire-proven — 'gNB Some(788)' = free-ran-ue's gnbId 000314 from its real NG Setup; the HO itself needs 2 gNBs (not sim-drivable, precedent). Data forwarding + HO Prep Failure/Cancel + stale-entry expiry + inter-AMF (N14) deferred |
| [82-handover-data-forwarding.md](82-handover-data-forwarding.md) | Data forwarding during N2 handover — DIRECT forwarding (TS 23.502 §4.9.1.3.2; design/81 dropped every in-flight DL packet). ngap: handover_required gains direct_forwarding:bool (HandoverRequiredTransfer.direct_forwarding_path_availability = direct-path-available; parser folds any-session→true); handover_request_acknowledge admitted items become (psi,dl_teid,dl_addr,Option<(fwd_teid,fwd_addr)>) (dL-Forwarding-UP-TNL-Information in the ack transfer); handover_command gains forwarding:&[(psi,teid,addr)] → PDUSessionResourceHandoverList w/ HandoverCommandTransfer.dl_forwarding_up_tnl_information per session (IE omitted when empty; hand-built IEs since conditional). AMF: on_handover_required logs the availability; on_handover_request_ack SPLITS the ack — DL F-TEIDs → pending.admitted (UPF re-point at Notify, unchanged) vs forwarding F-TEIDs → relayed to the source in the Handover Command (the AMF's whole role: the forwarding traffic is RAN-to-RAN Xn-U, never touches the core). Tests: roundtrips (flag true/false, ack w/ fwd 0xBB/10.0.9.6, command list + omitted-when-empty); orchestration asserts the source's command carries [(5,0xBB,10.0.9.6)] while the Notify re-point still targets the REAL DL F-TEID 0xAA. 169 tests; BDD 5/5; not sim-drivable (2 gNBs, precedent). Indirect forwarding (UPF N4 tunnels) + per-QoS-flow granularity + HO failure paths + GTP-U end marker deferred |
| [83-handover-failure-paths.md](83-handover-failure-paths.md) | Handover failure paths (designs 79-82 built happy paths; failures were silent returns + LEAKED in-flight entries). ngap: handover_preparation_failure(+params) (UnsuccessfulOutcome HandoverPreparation, AMF→source, radio cause); handover_failure(+params) (UnsuccessfulOutcome HandoverResourceAllocation, target→AMF, NO ran id); handover_cancel/_acknowledge(+params); path_switch_request_failure(+params) (cause rides in each PathSwitchRequestUnsuccessfulTransfer — no message-level Cause IE per TS 38.413). AMF: on_handover_required ANSWERS failures (unknown UE→UNKNOWN_LOCAL_UE_NGAP_ID, unseeded chain→UNSPECIFIED, no target assoc→UNKNOWN_TARGET_ID); on_handover_failure (new UnsuccessfulOutcome arm) drops the entry + forwards prep-failure w/ the target's cause to the source; on_handover_cancel drops + releases the target's prepared ctx when ack'd (release cause HANDOVER_CANCELLED via target_tx) + HandoverCancelAcknowledge (idempotent for unknown); PendingHandover gains target_tx/target_ran_ue_id/commanded; TNGRELOCprep (10s, env RADIAN_AMF_TNGRELOCPREP_SECS, armed@Required, no-op once commanded) expiry → drop + prep-failure TNGRELOCPREP_EXPIRY; TNGRELOCoverall (20s, env …OVERALL_SECS, armed@Ack) expiry → drop + target release TNGRELOCOVERALL_EXPIRY; Notify consuming the entry stops both; on_path_switch unknown/unseeded → PathSwitchRequestFailure w/ released PSI list (was silent). Tests: 5 failure-message roundtrips; n2_handover_failure_paths_clean_up (5 scenarios: unknown target, target rejection, post-ack cancel w/ target release, prep expiry, overall expiry w/ prep-no-op-once-commanded); design/79 test asserts the failure responses. 171 tests; BDD 5/5; not sim-drivable (precedent). Failure transparent containers not relayed; UE stays on source on failure (burned NH rotation = spec cost); indirect forwarding still deferred |
| [84-indirect-data-forwarding.md](84-indirect-data-forwarding.md) | Indirect data forwarding during N2 handover (TS 23.502 §4.9.1.3.3; design/82 = direct/Xn-U only, no-direct-path sources dropped in-flight packets). First handover path where the CORE's user plane carries forwarded traffic: source → UPF → target. pfcp: session_establishment_request_indirect_forwarding (one PDR match a UPF-allocated Access F-TEID + FAR forward-to-Access w/ OHC to the TARGET, set at establishment); the existing UPF handle_n4 allocates+returns the ingress F-TEID unchanged (session-shape-agnostic — NO UPF change; pfcp test drives the REAL handler). nf-smf: SmContext.indirect_fwd:Option<u64>; POST …/{sm_ref}/indirect-forwarding — {targetN3Teid,targetN3Addr}→establish + return {fwdN3Teid,fwdN3Addr} (UPF ingress); {release:true}→delete (idempotent 204). nf-amf: AmfSmf::{setup,release}_indirect_forwarding (AmfSmf+NrfClient now derive Clone — the expiry timer owns one); PendingHandover gains direct_forwarding + sessions:Vec<(psi,(sm_ref,smf_base))> (the ack lands on the TARGET assoc where the UE ctx is out of reach) + indirect_active; on_handover_request_ack now ASYNC — direct → command carries the target's fwd F-TEIDs (design/82); else per admitted forwarding session, setup_indirect_forwarding → put the UPF INGRESS F-TEID in the command instead; teardown (release_indirect_forwarding) on Notify completion + source cancel-after-ack + TNGRELOCoverall expiry (idempotent, skipped when nothing set up). Tests: pfcp real-UPF forwarding-session establish+F-TEID-alloc+delete; nf-amf n2_handover_sets_up_indirect_forwarding (no direct path → 1 tunnel at mock SMF, command carries UPF 000000cc/10.0.9.9 not target 0xBB, indirect_active, released on notify). 173 tests; BDD 5/5 (normal datapath intact). BOUNDARY: no forwarding-drain timer (released instantly at Notify — in-flight may drop), per-session not per-QoS-flow, UPF forwarding datapath not exercised end-to-end (no 2-gNB live path), no GTP-U end marker. Forwarding drain timer + per-flow granularity deferred |
| [85-periodic-registration.md](85-periodic-registration.md) | Periodic registration updating (TS 24.501 §5.5.1.3.2) — closes a design/76 CORRECTNESS HOLE: a periodic RegistrationRequest (type 011) fell through the CM-IDLE-return classifier + was DROPPED, so a CM-IDLE UE checking in when T3512 expired was STILL evicted by the design/66 implicit-dereg sweep (opposite of the point). Now accepted lightweight (no re-auth), refreshing the retained context. nas: registration_request_of_type(reg_type,mcc,mnc,tmsi) shared GUTI builder (factored from design/76 mobility) + registration_request_periodic (type PeriodicRegistrationUpdate); mobility builder now delegates. nf-amf: on_service_request classifier gains is_periodic; is_registration_update = mobility ∪ periodic drives the shared restore+ICS+RegistrationAccept+no-UP-reactivation path; the only diffs — mobility RE-ASSIGNS the area (registration_area_for), periodic KEEPS it unchanged (UE hasn't moved), Service Request still extends-if-outside; accept label '…(RegistrationAccept — periodic)'. The return removes from RETAINED (clears retained_at) + restores CM-CONNECTED → periodic reg REFRESHES the implicit-dereg deadline (re-idles later w/ fresh retained_at). Tests: nas periodic roundtrip; nf-amf periodic_registration_update_refreshes_without_reauth (retained UE area [000001,000002]/retained_at set/1 session → 1 downlink ICS(RegistrationAccept—periodic), 0 UP reactivation, restored CM-CONNECTED + retained_at None + area UNCHANGED + session kept + UE verifies accept same-area/kept-NSSAI, RETAINED empty). 174 tests; BDD 5/5; not sim-drivable (free-ran-ue can't go CM-IDLE, precedent). BOUNDARY: no auto AN-release after accept (UE/AN drives re-idle), security ctx continues (no re-auth), no T3512 renegotiation. GUTI reallocation + Uplink Data Status + explicit-AN-release-after-periodic deferred |
| [86-guti-reallocation.md](86-guti-reallocation.md) | GUTI reallocation on a registration update (TS 24.501 §5.4.1.3; designs 76/85 kept the same GUTI — noted boundary). A registration update (mobility/periodic, NOT Service Request) now reassigns a fresh 5G-TMSI = this connection's AMF-UE-NGAP-ID (same scheme as initial registration, design/77). nas: guti_tmsi_from_registration_accept (reads the accept's 5G-GUTI IE, mirrors the request-side parser). nf-amf on_service_request: reg_tmsi = is_registration_update ? amf_ue_id-as-u32 (+ GUTI_DIRECTORY re-key: drop SUPI's old tmsi, insert new→SUPI + ctx.guti_tmsi=new) : tmsi; Registration Accept carries reg_tmsi; RETAINED re-keys NATURALLY on the next AN release (retains under ctx.guti_tmsi) + the UE's next Service Request presents the new 5G-S-TMSI → finds the re-keyed ctx. Tests: nas accept-GUTI roundtrip; nf-amf periodic test extended (accept carries NEW tmsi ≠ presented, ctx.guti_tmsi=new, GUTI_DIRECTORY old-removed/new→SUPI, UE reads new GUTI via guti_tmsi_from_registration_accept). 174 tests; BDD 5/5 (initial-reg @sim unaffected); not sim-drivable (free-ran-ue can't re-register, precedent). BOUNDARY: no Registration Complete tracking (new GUTI committed immediately, old not kept parallel-valid), always-reallocates (real AMF = operator policy), tmsi=AMF-UE-NGAP-ID single-AMF scheme. Registration Complete tracking + reallocation policy + Uplink Data Status deferred |
| [87-uplink-data-status.md](87-uplink-data-status.md) | Uplink Data Status — inline UP reactivation on a registration update (TS 24.501 §9.11.3.57; LAST design/76 follow-up). Designs 76/85 left a reg update's UP deactivated (only Service Request reactivated); now a UE flags pending-uplink-data sessions in the Uplink Data Status IE (IEI 0x40) + the AMF reactivates THOSE inline. nas: uplink_data_status_value(psis)/psis_from_uplink_data_status(value) (2-octet PSI bitmap, octet3=PSI0-7/octet4=PSI8-15, PSI0 spare); registration_request_of_type gains uplink_data_psis param (sets the IE when non-empty; mobility/periodic wrappers pass &[]) + registration_request_with_uplink_data; uplink_data_status_from_registration_request parser (reg.uplink_data_status raw TLV). nf-amf on_service_request: reactivate set = Service Request → ALL sm_refs (unchanged); registration update → sm_refs filtered to the Uplink Data Status PSIs (was &[] always); no-IE update reactivates nothing (design/76/85 behavior). Reactivated sessions run the existing resume path (ACTIVATING → N2 PDU Session Resource Setup); update-with-UDS downlinks = [ICS(RegistrationAccept), PDUSessionResourceSetupRequest ×listed]. Tests: nas bitmap encoding (PSI5+8→[0x20,0x01]) + IE roundtrip (5,9); nf-amf registration_update_reactivates_uplink_data_status_sessions (retained UE 2 sessions [5,6], mobility update flags PSI5 → [RegAccept, 1 PDU setup], mock SMF exactly 1 ACTIVATING, PSI6 stays deactivated). 176 tests; BDD 5/5; not sim-drivable (precedent). BOUNDARY: no PDU Session Status cross-check (trusts UDS vs retained sm_refs), Service Request still reactivates ALL (not its own UDS), no Allowed PDU Session Status in accept. PDU Session Status reconcile + UDS-on-Service-Request + Allowed-PDU-Session-Status deferred |
| [88-ics-inline-pdu-sessions.md](88-ics-inline-pdu-sessions.md) | Inline PDU sessions in the Initial Context Setup (TS 38.413 §9.2.2.1) — closes the design/78 boundary: a CM-IDLE resume re-established the AS context with an ICS but the reactivated sessions TRAILED as separate PDUSessionResourceSetupRequest messages; now they ride the ICS inline (PDU Session Resource Setup List Cxt Req) in ONE procedure + the gNB's ICS Response carries their per-session results. ngap: InitialContext.pdu_sessions:Vec<IcsPduSession{psi,flows,upf_teid,upf_addr}> → each maps to a PDUSessionResourceSetupItemCxtReq reusing the existing setup-request transfer (UPF UL N3 F-TEID + QoS flows, no per-session N1 — the accept is the ICS NAS-PDU); QosFlow/Gbr gain PartialEq; initial_context_setup_request_session_ids parser (request's inline sessions); initial_context_setup_response_with_sessions + initial_context_setup_session_ids (gNB ICS Response's PDUSessionResourceSetupListCxtRes → each session's DL N3 F-TEID). AMF: on_service_request gathers the reactivated sessions (Nsmf ACTIVATING → UPF UL F-TEID, as before) BEFORE building the ICS + puts them in InitialContext.pdu_sessions → a resume emits a SINGLE InitialContextSetupRequest (no trailing setups); new on_initial_context_setup_response (the ICS-Response handle_ngap arm) parses the inline results + drives UpdateSMContext per gNB DL F-TEID (same downlink-install the standalone PDUSessionResourceSetupResponse path does); no-K_AMF fallback (unreachable) still degrades to DL-NAS + trailing setups. Registration-time ICS (design/77) unchanged (no sessions yet). The design/87 Uplink Data Status reg-update sessions now also ride inline. Tests: ngap initial_context_setup_roundtrips extended (inline session round-trips via _request_session_ids; gNB response DL F-TEID via _session_ids; no-session → empty); nf-amf service_request_resumes_a_cm_idle_ue (single ICS carrying PSI 5 inline w/ UPF UL F-TEID, no trailing setup) + registration_update_reactivates_uplink_data_status_sessions (UDS session rides the ICS) + ics_response_installs_inline_session_downlinks (gNB DL F-TEID 0xAB/10.0.1.2 for PSI 5 → 1 UpdateSMContext; session-less response installs nothing). 177 tests; BDD 5/5 (live @sim registration ICS + standalone PDU setup unaffected — inline encoding reuses the same setup transfer free5gc decodes); resume not sim-drivable (free-ran-ue can't go CM-IDLE, precedent). BOUNDARY: no PDUSessionResourceFailedToSetupListCxtRes handling (admitted list only), fixed single S-NSSAI (sst 1). Failed-to-setup handling + per-session S-NSSAI deferred |
| [89-ics-failed-to-setup.md](89-ics-failed-to-setup.md) | Failed-to-setup handling on the InitialContextSetupResponse (TS 38.413 §9.2.2.2 — closes the design/88 boundary: the AMF installed downlinks from the ICS Response's ADMITTED list but silently ignored the FAILED list, leaving the SMF/UPF with a session the RAN never established that the AMF would try to reactivate on the next resume). Now each rejected session is released at the SMF + dropped from the UE context. ngap: initial_context_setup_response_with_results(amf,ran,admitted:&[(u8,u32,Ipv4Addr)],failed:&[(u8,u8)]) — the general ICS Response builder (admitted → PDUSessionResourceSetupListCxtRes; failed=(psi,radio-network cause) → PDUSessionResourceFailedToSetupListCxtRes, each cause in a PDUSessionResourceSetupUnsuccessfulTransfer; either list omitted when empty); initial_context_setup_response_with_sessions now a thin wrapper (no failures). initial_context_setup_failed_session_ids(pdu)→Vec<(u8,u8)> parses the failed list; private cause_value(&Cause)→u8 reads the numeric out of any Cause group for logging. nf-amf on_initial_context_setup_response: after the admitted-downlink loop (design/88), iterate the failed list — per rejected PSI look up the tracked SM context, release_sm_context (tears down the SMF/UPF datapath), remove from ctx.sm_refs (get/clone before await, get_mut after). BOUNDARY: cause logged only (action=release regardless; group dropped), no retry, UE not told here (Allowed PDU Session Status / N1 release is the design/87 PDU-Session-Status follow-up). Tests: ngap initial_context_setup_roundtrips extended (response admits 5 + rejects 6 → both lists round-trip, cause MULTIPLE_PDU_SESSION_ID_INSTANCES reads back, all-admitted → empty failed); nf-amf ics_response_installs_inline_session_downlinks extended (gNB admits PSI 5 install + rejects PSI 6 → mock SMF sees 1 release for PSI 6's ctx, PSI 6 dropped from sm_refs, PSI 5 kept; session-less response releases nothing). 177 tests; BDD 5/5 (live @sim registration ICS carries no inline sessions/failures — happy path untouched); failed-list not sim-drivable (free-ran-ue admits its sessions, design/64/65 precedent). NEXT: per-session S-NSSAI; UE-side reconciliation (Allowed PDU Session Status / N1 release); retry a rejected session on alternative resources |
| [90-pdu-session-status-reconcile.md](90-pdu-session-status-reconcile.md) | PDU Session Status reconciliation on a CM-IDLE return (TS 24.501 §9.11.3.44 — closes the design/87 & design/89 gap: the AMF never acted on the UE's PDU Session Status IE, and never told the UE which sessions the network kept). Wires the PDU Session Status IE BOTH ways on a Service Request / registration update. nas: the two-octet PSI bitmap is now shared (psi_bitmap_value/psis_from_psi_bitmap, renamed from the design/87 uplink-data pair) between Uplink Data Status + PDU Session Status; pdu_session_status_from_request(msg)→Option<Vec<u8>> (AMF parses the UE's active set from a ServiceRequest OR RegistrationRequest; None=IE absent=keep all, Some(possibly-empty)=authoritative); service_accept(active:Option<&[u8]>) + registration_accept(…,active:Option<&[u8]>) gained the network's PDU Session Status IE (0x50, included when Some); service_request_with_pdu_status (UE side) + pdu_session_status_from_accept (UE parses the network's set). nf-amf on_service_request: BEFORE the sm_refs snapshot — (1) UE→network: if the trigger carried the IE, release_sm_context each tracked session the UE did NOT list + drop from ctx.sm_refs (absent IE reconciles nothing); (2) network→UE: the accept advertises the reconciled active set (sorted ctx.sm_refs keys) so the UE drops what the network released. Reactivation set (design/87/88) computed AFTER reconciliation so a dropped session is neither reactivated nor advertised. Initial registration passes None (no sessions yet — live @sim Registration Accept unchanged). BOUNDARY: design/89's ICS-Response rejection still resolved by SMF release (UE learns at its NEXT return via that return's status, no mid-connection N1 release to a connected UE); Allowed PDU Session Status IE (§9.11.3.13) not modelled. Tests: nas pdu_session_status_reconciliation_ies (ServiceRequest/Accept + RegistrationAccept round-trip the IE; plain request/minimal accept → None); nf-amf service_request_reconciles_dropped_pdu_session (retained UE sessions {5,6} resumes w/ Service Request listing only 5 → session 6 released at mock SMF, only 5 reactivated inline in the ICS, sm_refs ends {5}, Service Accept advertises PDU Session Status [5] which the UE decodes). 179 tests; BDD 5/5 (live registration unchanged — None); not sim-drivable (free-ran-ue can't go CM-IDLE / drop sessions, design/64/65 precedent). NEXT: mid-connection N1 release (tell a connected UE immediately); Allowed PDU Session Status on the Service Request; per-session S-NSSAI |
| [91-network-pdu-session-release.md](91-network-pdu-session-release.md) | Network-initiated PDU session release (TS 23.502 §4.3.4 — closes the design/90 boundary: a CONNECTED UE was never told mid-session when the network dropped a session). The N2/N1 counterpart to the design/50 modification: the SMF asks the AMF to release a session, the AMF tears down RAN resources (N2) + tells the UE (N1), the gNB confirms, the AMF finalises at the SMF. nas: pdu_session_release_command(psi,pti,cause) (5GSM 0xD3 + mandatory cause; network-initiated ⇒ PTI 0), pdu_session_release_complete(psi,pti) (0xD4, UE side) + is_pdu_session_release_complete(container) (0xD4 @ offset 3 detector); sm_cause::REGULAR_DEACTIVATION (#36). ngap: pdu_session_resource_release_command(amf,ran,psi,nas) (§9.2.1.6 — N1 rides as NAS-PDU, per-session transfer cause = NAS normal-release) + nas_pdu_from_release_command (gNB side → (psi,N1)); pdu_session_resource_release_response(amf,ran,psi) (test/sim) + release_response_result parser. nf-amf: SBI callback POST /namf-comm/v1/ue-contexts/{supi}/release ({pduSessionId,cause?}) → UE_DIRECTORY → UeCmd::ReleaseSession{amf_ue_id,psi,cause} to the owning association (202/404, mirrors modify_policy); on_network_release builds the N1 Release Command (protected DL NAS) + N2 Release Command (session stays tracked until the gNB confirms); on_release_response (new PDUSessionResourceReleaseResponse handle_ngap arm) → release_sm_context at the SMF + drop from ctx.sm_refs; the UE's PDU Session Release Complete (0xD4 over UL NAS) is now recognised in dispatch_uplink_nas + acknowledged (no longer falls through to the establishment/CreateSMContext path). BOUNDARY: session finalised at the SMF on the N2 Release Response not the UE's N1 complete (simplification; strict §4.3.4 = N4 delete after the complete); CM-IDLE targets not handled (releases a connected UE; idle sessions reconciled at the next return, design/90); single session per command, fixed normal-release N2 cause. Tests: nas pdu_session_release_command_and_complete_layout (0xD3/0xD4 octets + detector + UL-NAS release-complete round-trip); ngap pdu_session_resource_release_roundtrips (command carries N1+psi, response reports the released session); nf-amf network_release_tears_down_session_ran_and_ue (N2 Release Command carries a UE-decodable N1 0xD3/cause #36; on the Release Response the mock SMF sees the release + session drops from sm_refs; absent-session release is a no-op). 182 tests; BDD 5/5 (live @sim datapath unaffected — no release triggered); not sim-drivable (free-ran-ue exposes no network-release trigger, design/64/65 precedent). NEXT: finalise on the N1 Release Complete (strict ordering) + release timer/retransmission; multi-session release; CM-IDLE release |
| [92-release-finalize-on-complete.md](92-release-finalize-on-complete.md) | Finalise a PDU session release on the UE's N1 Complete (TS 23.502 §4.3.4 — closes the design/91 boundary: it finalised at the SMF on the N2 Release Response and treated the UE's N1 Release Complete as a bare ack). Now the SMF finalisation (N4 delete / IP release) follows the UE's complete, with a guard timer as a backstop. nf-amf: UeContext.releasing:HashSet<u8> (sessions with a release outstanding, awaiting the complete); on_network_release marks the session releasing + arms a guard timer (arm_release_guard, mirrors arm_t3522, RELEASE_GUARD_SECS=6 env RADIAN_AMF_RELEASE_GUARD_SECS) when it sends the Release Command; on_release_response (N2 response) no longer finalises — now SYNC, logs the gNB freed RAN resources + leaves the session tracked awaiting the complete; finalize_release(ues,amf_smf,amf_ue_id,psi) shared IDEMPOTENT finaliser (release_sm_context at SMF + drop from sm_refs & releasing; no-op if not releasing → a late guard after the complete, or a stray complete, does nothing); dispatch_uplink_nas release-complete branch (0xD4) now calls finalize_release (strict-ordering finalisation point); UeCmd::ReleaseGuardExpiry{amf_ue_id,psi} + select-loop arm (finalize_release if still releasing — the UE never answered). Flow: Release Command (mark releasing + arm guard) → N2 Release Response (RAN freed, still pending) → UE N1 Release Complete → finalize_release; guard fires finalize_release if no complete arrives. BOUNDARY: guard FINALISES on expiry (not retransmit-then-finalise — one attempt, then finalise to avoid a stranded UPF datapath); per-(UE,psi) one-shot guard (re-arm last-wins, idempotent finaliser keeps SMF release single); still single-session per command + CM-CONNECTED only (design/91). Tests: nf-amf network_release_finalises_on_ue_complete (N2 Release Response frees RAN but mock SMF sees NO release + session stays tracked; UE N1 Release Complete over UL NAS via dispatch_uplink_nas finalises → SMF release, sm_refs/releasing cleared; late guard firing idempotent — no double release), network_release_guard_finalises_a_silent_ue (no complete → guard's finalize_release releases at the SMF anyway). Shared release_test_smf harness uses a per-test Arc<Mutex> via axum State (the two tests run in parallel — a shared static raced). 183 tests (nf-amf 42, +1 net); BDD 5/5 (live datapath unaffected); not sim-drivable (free-ran-ue exposes no network-release trigger, design/64/65 precedent). NEXT: retransmit the Release Command on the guard (bounded count) before finalising; multi-session release; CM-IDLE release |
| [93-cm-idle-pdu-release.md](93-cm-idle-pdu-release.md) | Network-initiated PDU session release for a CM-IDLE UE (TS 23.502 §4.3.4 — closes the design/91/92 boundary: release worked only for a CM-CONNECTED UE; a release for a CM-IDLE UE fell through to 404, retained session never torn down). With no N2 to signal, the AMF releases the retained session at the SMF now + drops it from the retained context; the design/90 PDU Session Status reconciliation informs the UE on its next return. nf-amf ONLY (no nas/ngap change): the release_session callback (POST /namf-comm/v1/ue-contexts/{supi}/release) now has two arms — CM-CONNECTED (UE_DIRECTORY hit, unchanged: UeCmd::ReleaseSession → owning association → full N2/N1 procedure, designs 91/92) and CM-IDLE (directory miss OR association closed mid-call → find the session in RETAINED by SUPI, release_sm_context at the SMF (tear down UPF datapath now, reuses the evict_stale_retained pattern, constructs AmfSmf from NRF_BASE), drop the PSI from the retained ctx.sm_refs → the accept's PDU Session Status on the UE's next return omits it so the UE releases locally); 404 only when the UE holds no such session (neither connected nor retained with that PSI). Lock discipline: find (tmsi, (sm_ref, smf_base)) under the RETAINED lock, release_sm_context off-lock, re-lock to remove the PSI. BOUNDARY: no paging (the session is going away — the natural next return reconciles it, §4.3.4 permits release-without-paging for CM-IDLE); release-racing-a-resume window (if the UE resumes between the retained lookup and the PSI removal it may reactivate the session as the SMF tears it down — the removal then no-ops; common idle-stays-idle case clean); retained context otherwise intact (only the released PSI dropped). Tests: nf-amf cm_idle_pdu_session_release (retained CM-IDLE UE sm_refs{5,6} NOT in UE_DIRECTORY, drives namf_callback_router() over h2c; POST {pduSessionId:5} → 202, mock SMF (via the shared release_test_smf harness) sees "ctx-idle-5" released, RETAINED[tmsi].sm_refs drops 5 keeps 6; POST {pduSessionId:9} → 404). 184 tests (nf-amf 43, +1); BDD 5/5 (live datapath unaffected — no release triggered); not sim-drivable (free-ran-ue can't go CM-IDLE / exposes no release trigger, design/64/65 precedent). NEXT: reactivate-race hardening (mark the retained session release-pending so a concurrent resume skips it); multi-session release list; retransmit the Release Command on the guard (design/92 backlog) |
| [94-multi-session-release.md](94-multi-session-release.md) | Multi-session PDU release in one request (closes the design/91-93 single-session-per-request boundary). One SMF request naming several sessions → the AMF releases each; one N2 Release Command per session (the N2 command carries a single N1), each finalising independently (design/92). nf-amf ONLY: the release_session callback parses a LIST — pduSessionIds (array) OR the single pduSessionId (unchanged for existing callers); empty→400. UeCmd::ReleaseSession carries psis:Vec<u8> (was psi:u8). on_network_release(ues,amf_ue_id,psis:&[u8],cause,tx) loops → one ngap::pdu_session_resource_release_command (+ N1) PER requested session, each ctx.releasing.insert(psi) + arm_release_guard, unknown sessions skipped; returns one downlink per session (ctx.sec checked once up front, protect advances the DL count per message). CM-IDLE path: releases each requested session found in the retained ctx at the SMF + drops from sm_refs (gather under RETAINED lock, release off-lock, re-lock to remove) so design/90 reconciliation omits them on return; 404 only when the UE holds NONE of the requested sessions (else 202). Each session finalises independently on its own N1 Release Complete OR guard expiry (design/92) — a multi-release is N concurrent single-session procedures sharing one trigger. BOUNDARY: one N2 command per session (not one listing all PSIs — the N2 command carries a single NAS-PDU/N1; the wire PDUSessionResourceToReleaseListRelCmd stays single-item); partial success (CM-CONNECTED 202 once queued, unknown PSIs skipped+logged in the association task; CM-IDLE 202 if ANY released); one shared cause for the batch. Tests: nf-amf multi_session_release_fans_out_per_session ([5,6]→2 N2 Release Commands each w/ its own N1 for a distinct psi via nas_pdu_from_release_command, both releasing; each finalised via dispatch_uplink_nas release-complete → mock SMF releases both, sm_refs empty), cm_idle_pdu_session_release extended (pduSessionIds:[6,9] releases held 6 skips unheld 9 → 202, both idle sessions gone), design/91/92 single-session tests updated to &[psi]. 185 tests (nf-amf 44, +1); BDD N6 datapath feature passes (2 scenarios); the @sim e2e scenario blocked LOCALLY this session by an UNRELATED zebra-rs process holding 127.0.0.8:8805 (collides with the host UPF's 0.0.0.0:8805 N4 bind → datapath UPF won't start; release path not exercised by @sim) — CI verifies the full @sim run; not otherwise sim-drivable (free-ran-ue exposes no release trigger). NEXT: reactivate-race hardening (design/93); retransmit the Release Command on the guard (design/92); per-session cause in one request |
| [95-pfcp-network-instance.md](95-pfcp-network-instance.md) | PFCP Network Instance (DNN) on the forwarding rules (interop gap.txt Gap 1 — radian emitted ZERO NetworkInstance IEs, so a downstream MUP/SRv6 backhaul controller that maps a session→VRF by its Network Instance=DNN originated no routes for any radian session). Carries the session's DNN as the PFCP NetworkInstance (TS 29.244 §8.2.4) on every forwarding rule, mirroring free5GC. crates/pfcp: session_establishment_request gained dnn:&str — both FARs now .forward_to_network(Interface::Core/Access, NetworkInstance::new(dnn)) (was .forward_to(Interface::…)) so uplink(Core)+downlink(Access) FAR each carry NI=DNN; session_modification_request gained dnn:&str — its UpdateForwardingParameters now re-sends DestinationInterface(Access) + NetworkInstance(DNN) alongside the gNB OuterHeaderCreation (Update FAR fully specified, matching free5GC, folds in gap.txt note #3; keeps DNN→VRF visible on every downlink re-point: activation/handover/path-switch). nf-smf: SmContext gained dnn:String (set from req.dnn at creation); establishment passes &req.dnn, update_sm_context reads c.dnn (so activations, SR resumes, handover downlink re-points all carry the DNN). The radian UPF ignores the IE (allocates N3 F-TEID regardless — datapath unchanged); the IE is for a downstream controller reading the PFCP stream. BOUNDARY: NI on the FAR forwarding params (not the PDI — where free5GC sets it + the controller reads it); indirect-forwarding session (design/84) left without NI (transient RAN→UPF→RAN, no DNN→VRF meaning); Gaps 2 (N3 F-TEID CHOOSE bit) + 3 (End Marker) remain. Tests: pfcp network_instance_carries_the_dnn (establishment uplink+downlink Create FARs both NI="internet"; modification Update FAR carries NI + destination interface + OHC); all callers updated to the new sigs (nf-smf/nf-upf/n6/bdd/pfcp tests, dnn "internet"). 186 tests (pfcp +1); clippy clean. BDD N6 DATAPATH FEATURE 2/2 GREEN — the real end-to-end check: drives a live PFCP session establishment + modification (now carrying NI) through radian's UPF in a namespace + the packet round-trips (IE doesn't disturb the datapath); the @sim e2e scenario remained blocked LOCALLY by the unrelated zebra-rs process holding the host UPF's N4 port 127.0.0.8:8805 (design/94 gotcha) — CI verifies it. NEXT: Gap 2 (set CHOOSE/CH bit on the uplink PDI F-TEID vs the CH=0 placeholder + Created-PDR readback); Gap 3 End Marker on a downlink path switch; per-DNN NI naming policy |
| [96-pfcp-fteid-choose.md](96-pfcp-fteid-choose.md) | PFCP uplink F-TEID CHOOSE flag (interop gap.txt Gap 2 — radian's SMF sent a zero-address placeholder uplink PDI F-TEID Fteid::ipv4(0,smf_ip) with CH=0 (SMF-assigned semantics, TS 29.244 §8.2.3) yet expected the UPF to allocate the N3 F-TEID + report it in the Created PDR → non-standard; a strict UPF honouring CH=0 wouldn't allocate). Now sets the CHOOSE (CH) flag — the standard "UPF-assigned F-TEID" signal. crates/pfcp: new helper upf_chooses_fteid() = FteidBuilder::new().choose_ipv4().teid(0).build() (CH+V4 set, no SMF-assigned address; teid(0) is a required builder placeholder, the CH flag marks it as not the assignment); both uplink-PDI placeholders use it — the main session_establishment_request (N3) + session_establishment_request_indirect_forwarding (indirect-forwarding ingress). The UPF is UNCHANGED — handle_n4 already allocates the F-TEID + returns a Created PDR (now in response to a proper CH signal, not a placeholder); the SMF still reads the allocated F-TEID from the Created PDR, so the radian SMF↔UPF handshake is unchanged in effect but standardly signalled, and a strict/free5GC-style UPF is now correctly told to allocate. BOUNDARY: rs-pfcp ENCODING QUIRK — it emits the 4-octet TEID field even for a CHOOSE F-TEID (strict TS 29.244 omits it when CH=1; the CH flag is the signal a peer acts on; rs-pfcp↔rs-pfcp self-consistent but a strict decoder should be checked to tolerate the extra 4 octets — flag for the MUP-C); the UPF still allocates regardless of CH (full symmetry — UPF honouring CH=0 to accept an SMF-assigned F-TEID, for radian-UPF↔free5GC-SMF interop — is a follow-up, not needed for the MUP-C which is a UPF); Gap 3 End Marker remains. Test: pfcp uplink_fteid_requests_upf_allocation (uplink PDR id 1's PDI F-TEID has ch+v4 set + no address; end-to-end handle_n4 still allocates a non-zero TEID + reports its chosen N3 address in the Created PDR, parse_session_establishment_response reads it back). 187 tests (pfcp +1); clippy clean. BDD 5/5 GREEN — the full @sim e2e ran this run (:8805 free): a live PDU session (SMF sends CHOOSE F-TEID → UPF allocates → SMF installs datapath) + the UE pings the data network — end-to-end proof through radian's real SMF↔UPF. NEXT: UPF honours CH (allocate on CH=1, accept SMF-assigned on CH=0); Gap 3 End Marker; strict TS 29.244 CHOOSE encoding (rs-pfcp fix / hand-marshal) |
| [97-pfcp-end-marker.md](97-pfcp-end-marker.md) | GTP-U End Marker on a downlink path switch (interop gap.txt Gap 3's only real sub-gap — Gap 3 was LARGELY a misread since radian already re-programs the downlink FAR OHC on handover/path-switch designs 79-84, but it never asked the UPF to send a GTP-U End Marker on the old path; free5GC does, so the target gNB delivers downlink in order across the move). crates/pfcp: session_modification_request gained send_end_marker:bool — when set, adds the PFCPSMReq-Flags IE (rs-pfcp PfcpsmReqFlags, IeType 49) with the SNDEM bit (Send End Marker Packets, TS 29.244 §8.2.79) via builder.pfcpsm_req_flags(Ie::new(IeType::PfcpsmReqFlags, PfcpsmReqFlags::SNDEM.marshal().to_vec())). nf-smf update_sm_context: fetches the session's current gNB target (added c.gnb to the context read) + sets send_end_marker = old_gnb.is_some_and(|g| g != (gnb_teid,gnb_addr)) — true ONLY when the downlink is re-pointed from an EXISTING, DIFFERENT gNB tunnel (handover/Xn path switch); a first activation or Service-Request re-activation (no prior gNB, or the same one) sends none. Since path-switch (design/79) + N2 handover (design/81) both drive update_sm_context with the new gNB F-TEID, the End Marker is requested on every real downlink move automatically. BOUNDARY: SMF-side SIGNALLING only — radian's own UPF doesn't yet ACT on SNDEM (emit the GTP-U End Marker packet); the flag is for a downstream UPF (the MUP-C); radian-UPF-emits-End-Marker is a follow-up (only observable on a real 2-gNB handover, not sim-drivable); no End Marker count/retransmission modelled. rs-pfcp ApplyAction is the 1-octet form (DROP/FORW/BUFF/NOCP/DUPL — no End Marker bit); the End Marker is signalled via PFCPSMReq-Flags NOT ApplyAction. Tests: pfcp end_marker_requested_only_on_a_repoint (plain install → no PfcpsmReqFlags IE; re-point(true) → IE present with SNDEM set); nf-smf integration test EXTENDED (after installing downlink at gNB 0x5678/10.0.0.9, a 2nd UpdateSMContext to a DIFFERENT gNB 0x9abc/10.0.0.10 — the re-point carrying the End Marker request — is accepted by the real UPF + downlink_for(1) follows to (0x9abc,10.0.0.10), proving the SNDEM-carrying modification doesn't disturb the UPF); all session_modification_request callers updated (default false). 188 tests (pfcp +1); clippy clean. BDD N6 datapath feature 2/2 green (live PFCP establishment + modification through the real UPF, datapath unaffected — the datapath modify is a first-activation so send_end_marker=false); @sim e2e blocked LOCALLY by the recurring zebra-rs process on 127.0.0.8:8805 (design/94 gotcha, toggles as the user's zebra-rs restarts — now pid 11906) — CI verifies it; the End Marker needs a 2-gNB handover (not sim-drivable), integration-tested via the nf-smf re-point. gap.txt status: Gap 1 DONE (95), Gap 2 DONE (96), Gap 3's real sub-gap (End Marker signalling) DONE (97). NEXT: UPF emits the GTP-U End Marker (honour SNDEM — msg type 254 to the old gNB N3 tunnel before switching); End Marker count/retransmission |

## One-paragraph summary

For a 5G **core**, the ASN.1 dependency is small and is *not* the bottleneck:
~90% of the 5GC is HTTP/2 + JSON (the Service-Based Interfaces), and the only
ASN.1 the core proper needs is **NGAP** (N2), shared by the AMF (full PDU set)
and SMF (the N2-SM-info transfer-IE subset). Working Rust NGAP codecs already
exist (`oxirush-ngap`; or generate from TS 38.413 via `rasn` / `rasn-compiler`).
Everything else binary in the core is non-ASN.1 TLV: NAS (N1), PFCP (N4),
GTP-U (N3/N9). The RAN side (RRC/F1AP/E1AP/XnAP) is where ASN.1 cost would
explode — a separate, project-sized effort.

> Research dates: 2026-06-28. Spec/TS numbers are release-dependent; re-verify
> against the 3GPP release being targeted before locking interfaces.
